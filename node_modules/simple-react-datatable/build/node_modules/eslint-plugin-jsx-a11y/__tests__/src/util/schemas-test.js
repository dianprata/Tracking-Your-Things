'use strict';

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _schemas = require('../../../src/util/schemas');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* eslint-env jest */
describe('schemas', function () {
  it('should generate an object schema with correct properties', function () {
    var schema = (0, _schemas.generateObjSchema)({
      foo: 'bar',
      baz: _schemas.arraySchema
    });
    var properties = schema.properties || {};

    _assert2.default.deepEqual(properties.foo, 'bar');
    _assert2.default.deepEqual(properties.baz.type, 'array');
  });
});